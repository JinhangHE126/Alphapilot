# Extra optional dependencies for AlphaPilot document RAG.
# Core PDF table extraction (pdfplumber) is now in requirements.txt.
# Install these ONLY when you need camelot for complex/scanned tables:
#
#   pip install -r requirements-optional.txt
#
# Notes:
# - camelot-py requires a Java runtime and Ghostscript for lattice mode.
# - pdfplumber (in requirements.txt) is the recommended default — no Java needed.

camelot-py[cv]>=0.11.0
opencv-python-headless>=4.8.0
