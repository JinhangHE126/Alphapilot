langgraph
langgraph-checkpoint-sqlite
langgraph-supervisor
langchain-openai
langchain-google-genai
langchain-community
langchain-huggingface
langchain-core
sentence-transformers
faiss-cpu
yfinance
pandas
chromadb
python-dotenv
streamlit
pymupdf
markitdown>=0.0.1a3
gradio

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
python-multipart>=0.0.9
PyJWT>=2.8.0
aiohttp>=3.9.0
apscheduler>=3.10.0

# PDF upload (Phase 1): core text extraction — required for /api/upload/document
#   markitdown + pymupdf (above)
# Optional table extraction (annual reports with tables):
#   pip install -r requirements-optional.txt
#   (camelot-py needs Java; pdfplumber is the lightweight fallback)
